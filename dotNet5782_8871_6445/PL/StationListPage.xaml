<Page x:Class="PL.StationListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PL"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Station List Page">

    <Grid x:Name="MainGrid" Background="#c9cba3">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="UpGrid" HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">

        </Grid>
        <StackPanel Orientation="Horizontal">
            <!--<TextBlock Text="Order by:" Style="{StaticResource TextBlockStyle}"/>-->
            <!--<ComboBox x:Name="GroupBy" SelectionChanged="GroupingSelector_SelectionChanged" Style="{StaticResource ComboBoxBarStyle}"/>--> 
            <Button x:Name="AddNewStation" Content="Add new station" Click="AddNewStationButton_Click" Style="{StaticResource ButtonBarStyle}"/>
            <Button  x:Name="BackWindow" Content="Main Window" Click="BackWindow_Click" Style="{StaticResource ButtonBarStyle}"/>
        </StackPanel>

        <ListView x:Name="StationListView" Grid.Row="1" SelectionChanged="StationListView_SelectionChanged"  Style="{StaticResource ListViewStyle}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding ID}" Header="ID" Width="150"  />
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name"  Width="150"/>
                    <GridViewColumn DisplayMemberBinding="{Binding AvailableChargeSlots}" Header="Available slots"  Width="150"/>
                    <GridViewColumn DisplayMemberBinding="{Binding UsedChargeSlots}" Header="Used slots"  Width="150"/>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <EventSetter Event="UIElement.PreviewMouseLeftButtonDown" Handler="ListViewItem_PreviewMouseLeftButtonDown" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
